function getJeMoeder(naam: String, leeftijd: int) {
    type stoel {
        aantalPoten: int,
        isVanJeMoeder: bool,
        hhahaha: [6][10][1]moeder
    };

    x := y := (z := 345);

    return getJemoeder();
};

// Declare it

function foo() {
    x := 10;
};

function bar(x: int, y:int) {
    x := x^2;
    return x*y^2;
};

// Use it
foo();

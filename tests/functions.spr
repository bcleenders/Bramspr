function getJeMoeder(naam: String, leeftijd: integer) {
    type stoel {
        aantalPoten: integer,
        isVanJeMoeder: boolean,
        hhahaha: [6][10][1]moeder
    };

    x := y := (z := 345);

    return getJemoeder();
};

// Declare it

function foo() {
    x := 10;
};

function bar(x: integer, y:integer) {
    x := x^2;
    return x*y^2;
};

// Use it
foo();

x, y, z: integer;

z := 0; x := 1; y := 2;

if(x < y) {
	z := 10;
}

putInt(z);
putInt(10);
putChar('\n');

z := 0; x := 1; y := 2;

if(!( z < x)) {
	putChar('f');
	putInt(2);
}

if(x < x) {
	putChar('f');
	putInt(3);

}

if(z >= x) {
	putChar('f');
	putInt(4);

}